{
  "name": "token-login",
  "description": "Enable using a GitHub Actions OpenID Connect (OIDC) token in the NuGet credential provider.",
  "author": "NuGet Team",
  "private": true,
  "exports": {
    ".": "./dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bundle": "ncc build src/action/index.ts --license licenses.txt",
    "build": "tsc",
    "test": "jest"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "@types/node": "^12.7.11",
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.3.1",
    "jest-circus": "^29.3.1",
    "ts-jest": "^29.0.3",
    "typescript": "^5.3.2"
  }
}
